// Stub — retorna texto original até o Agent AI estar integrado
export async function reescreverComoMari(
  _conversaId: string,
  texto: string,
  _instrucao?: string,
): Promise<string> {
  return texto;
}
