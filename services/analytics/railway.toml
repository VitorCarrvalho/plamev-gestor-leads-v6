[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["src/**", "package.json", "../../packages/shared/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
numReplicas = 1
sleepApplication = false
healthcheckPath = "/api/analisar/funil"
healthcheckTimeout = 100
